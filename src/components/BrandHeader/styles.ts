// @/components/BrandHeader/styles.ts
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  // Vamos remover o container e estilizar apenas a imagem
  logoImage: {
    width: 420, // Tamanho maior para mais impacto
    height: 280, // Altura proporcional
    alignSelf: "center", // Garante que a imagem se centralize
  },
});
